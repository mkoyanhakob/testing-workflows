name: 'DAST API Scan'

on:
  push:

jobs:
  main:
    runs-on: ubuntu-latest

    steps:
      - name: 'Debug 1'
        run: |
          ls -la
    
      - name: 'ZAP Scan'
        uses: zaproxy/action-api-scan@v0.7.0
        with:
          target: 'https://my-dev.korioclinical.com'

      - name: 'Debug 2'
        run: |
          ls -la
